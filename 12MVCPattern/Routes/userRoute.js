const express = require("express");

// Instead of app use router from express
const router = express.Router();

const {} = require("../Controller/userController");

// Functions are imported from controller
router.route("/");
